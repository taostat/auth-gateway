{
  "name": "taostats-auth-gateway",
  "version": "1.0.0",
  "description": "OAuth 2.0 authentication gateway for Bittensor — wallet verification, RS256 JWTs, and on-chain role-based access control",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "node .esbuild.mjs",
    "dev": "nodemon --watch src --ext ts --exec 'tsx src/index.ts'",
    "start": "node dist/index.js",
    "test": "jest --forceExit --detectOpenHandles",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "generate-keys": "mkdir -p keys && openssl genrsa -out keys/private.pem 2048 && openssl rsa -in keys/private.pem -pubout -out keys/public.pem",
    "auth": "tsx scripts/cli-auth.ts",
    "demo": "DEMO_MODE=true ADMIN_API_KEY=demo-admin-key PUBLIC_URL=http://localhost:3000 VERIFICATION_URI=http://localhost:3000/v1/device/verify docker compose up --build",
    "setup-examples": "tsx examples/setup-demo-clients.ts",
    "migrate": "tsx -e \"import './src/db/migrate'; import { runMigrations } from './src/db/migrate'; runMigrations().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); })\""
  },
  "dependencies": {
    "@fastify/cors": "11.2.0",
    "@fastify/formbody": "8.0.2",
    "@fastify/rate-limit": "10.3.0",
    "@fastify/swagger": "9.7.0",
    "@fastify/swagger-ui": "5.2.5",
    "@polkadot/api": "15.10.2",
    "@polkadot/extension-dapp": "0.52.3",
    "@polkadot/keyring": "13.5.9",
    "@polkadot/util": "13.5.9",
    "@polkadot/util-crypto": "13.5.9",
    "dotenv": "16.6.1",
    "fastify": "5.8.1",
    "fastify-type-provider-zod": "4.0.2",
    "jsonwebtoken": "9.0.3",
    "pg": "8.18.0",
    "uuid": "11.1.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "@types/jsonwebtoken": "9.0.10",
    "@types/node": "22.19.10",
    "@types/pg": "8.16.0",
    "@types/uuid": "10.0.0",
    "esbuild": "0.25.12",
    "eslint": "9.39.2",
    "jest": "29.7.0",
    "nodemon": "3.1.11",
    "prettier": "3.8.1",
    "ts-jest": "29.4.6",
    "ts-node": "10.9.2",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.55.0"
  }
}
